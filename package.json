{
  "name": "spa-oauth",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "local": "npx vercel dev",
    "build": "vite build",
    "deploy:serverless": "vercel deploy --prod",
    "deploy": "npm run build && npm run deploy:serverless"
  },
  "devDependencies": {
    "nodemon": "^3.1.10",
    "vite": "^6.3.3"
  },
  "dependencies": {
    "express": "^5.1.0",
    "vercel": "^49.1.1",
    "vite-express": "*"
  }
}
